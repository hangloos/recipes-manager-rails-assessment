<% @recipe.ingredients.each do |ingredient| %>
  <%= ingredient.name %> | <%= ingredient.quantity %> | <%=link_to "Delete Ingredient", recipe_ingredient_path(ingredient) ,method: :delete, :confirm => "Are you sure you want to delete this ingredient" %>
  <br></br>
  <% end %>